const backendURL = "https://qtify-backend-labs.crio.do";

export const topAlbumsUrl = `${backendURL}/albums/top`;

export const newAlbumsUrl = `${backendURL}/albums/new`;

export const songsAlbumsUrl = `${backendURL}/songs`;

export const genresAlbumsUrl = `${backendURL}/genres`;

export const slugUrl = `${backendURL}/album/:slug`;
